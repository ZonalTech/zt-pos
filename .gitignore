.venv/
.claude/
__pycache__/
*.pyc
.env
*.log

# Uploaded product images (runtime data)
uploads/

# Installed license (per-machine runtime file, like .env)
license.key
# Private signing keys / issued licenses must never be committed
*-private-key.txt
*.lic

# Build artifacts
setup/
build/
dist/
installer/Output/
*.spec.bak
release/*.zip

# Dev/server runtime output
release/*.err
release/*.out

# zonal CLI runtime state + local DB backups
.zonal/
backups/
